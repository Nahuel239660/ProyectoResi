# src/graphql/mutations/createArtist.graphql
mutation CreateArtist($name: String!, $profileUrl: String, $bio: String) {
    createArtist(name: $name, profileUrl: $profileUrl, bio: $bio) {
       ...NewArtist
    }
}
